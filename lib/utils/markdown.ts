/**
 * Simple markdown parser that returns plain text
 * For now, just returns the original text to avoid JSX syntax issues
 */
export const parseMarkdown = (text: string): string => {
  return text
}
